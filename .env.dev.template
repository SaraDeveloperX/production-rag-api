# Production RAG API — Dev Environment
# Copy to .env and add your real keys.
# Do NOT commit this file.

# Secrets (required)
OPENAI_API_KEY=sk-your-openai-api-key-here
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX_NAME=your-dev-index

# Auth (off in dev)
REQUIRE_API_KEY=false
API_KEY=change_me

# Models
OPENAI_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small

# Retrieval
TOP_K=4

# Guardrails
BLOCKLIST=politics,violence

# Rate limit (dev default)
RATE_LIMIT=5/minute

# Data
DATA_DIR=./data
NAMESPACE=dev

# Evaluation (enable only for eval scripts)
EVAL_MODE=false

# Cache (off in dev)
CACHE_TTL_SECONDS=0
CACHE_MAX_ITEMS=256
